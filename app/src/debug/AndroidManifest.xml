<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only manifest overlay (R12, KD2, AE4 follow-up): declares the
  Compose shell's launcher entry point. The activity itself lives under
  `app/src/debug/java` (not `src/main/java`), so AGP only ever compiles it
  and the rest of `biz.logicminds.buelltune.ui` for the debug variant - a
  release build has no reachable component, launcher icon, or exported
  surface at all (R12: "reachable only from a debug-only entry point").
  The Compose runtime library itself still ships in a release build
  regardless (see `app/build.gradle.kts`'s `sourceSets.debug` comment for
  why). `MainActivity` and every other entry in the main manifest are
  untouched.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="biz.logicminds.buelltune.ui.BuellTuneDebugActivity"
            android:label="BuellTune Debug: Connection Status"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
